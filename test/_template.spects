import { expect, should } from 'chai';
import { __function__ } from '../src/__function__';

should();

describe(`__function__() - @category __category__`, () => {

  describe(`should ...`, () => {

    it(`__the_test_description_here`, () => {

      const orig: number[] = __original__input__;
      const input: number[] = orig.slice(0);
      const output: number[] = __function__(input);
      input.should.be.deep.equal(orig);
      output.should.not.be.equal(input);
      output.should.have.lengthOf(1);
      output.should.deep.equal(__expected__output__);

    });

  });

});
